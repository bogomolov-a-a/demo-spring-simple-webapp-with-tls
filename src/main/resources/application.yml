spring:
  profiles:
    active:
      - dev
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.artembogomolova.demo.webapp.dao.util.SQLLiteDialect
  datasource:
    driver-class-name: org.sqlite.JDBC
    continue-on-error: false
  liquibase:
    change-log: classpath*:db/changelog/db.changelog-master.yml
server:
  port: 8080